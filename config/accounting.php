<?php

return [
    /*
    |--------------------------------------------------------------------------
    | ضريبة القيمة المضافة الافتراضية (%)
    |--------------------------------------------------------------------------
    | تُستخدم عندما لا يوجد سجل في company_settings.default_vat_percent.
    | إن وُجدت قيمة في إعدادات المنشأة فهي تتقدّم على هذا المفتاح لكل الواجهات (View::share).
    */
    'default_vat_percent' => (float) env('DEFAULT_VAT_PERCENT', 15),

    /*
    |--------------------------------------------------------------------------
    | عرض الأرقام العشرية في الواجهات
    |--------------------------------------------------------------------------
    */
    'display_money_decimal_places' => (int) env('ERP_MONEY_DECIMALS', 2),
    'display_quantity_decimal_places' => (int) env('ERP_QUANTITY_DECIMALS', 2),

    /*
    |--------------------------------------------------------------------------
    | حسابات المخزون والتكلفة (أكواد دليل الحسابات)
    |--------------------------------------------------------------------------
    */
    'raw_materials_inventory_code' => env('ACCOUNT_RAW_MATERIALS', '1041'),
    'finished_goods_inventory_code' => env('ACCOUNT_FINISHED_GOODS', '1042'),
    'cogs_code' => env('ACCOUNT_COGS', '5000'),

    /*
    |--------------------------------------------------------------------------
    | المبيعات (ذمم مدينة / إيراد)
    |--------------------------------------------------------------------------
    */
    'accounts_receivable_code' => env('ACCOUNTS_RECEIVABLE_CODE', '1030'),
    'sales_revenue_code' => env('ACCOUNT_SALES_REVENUE', '4200'),

    /*
    |--------------------------------------------------------------------------
    | سندات صرف الموردين (الخزينة / الذمم الدائنة)
    |--------------------------------------------------------------------------
    */
    'treasury_account_code' => env('TREASURY_ACCOUNT_CODE', '1010'),
    'accounts_payable_code' => env('ACCOUNTS_PAYABLE_CODE', '2010'),

    /*
    |--------------------------------------------------------------------------
    | الحساب الأب في دليل الحسابات لإنشاء حساب فرعي لكل حساب بنكي جديد
    |--------------------------------------------------------------------------
    | الافتراضي 1020 (البنك — الحساب الرئيسي ضمن الأصول المتداولة) كما في DefaultLedgerAccounts.
    */
    'bank_ledger_parent_code' => env('BANK_LEDGER_PARENT_CODE', '1020'),
];
